
CREATE VIEW DUPECOUNT_VW AS
SELECT KEY_1, KEY_2, KEY_3, DUPECOUNT
	FROM (
	SELECT KEY_1, KEY_2, KEY_3, COUNT(ID) AS DUPE_COUNT
	FROM HAS_DUPLICATES
	GROUP BY KEY_1, KEY_2, KEY_3
	ORDER BY DUPE_COUNT DESC
	)
